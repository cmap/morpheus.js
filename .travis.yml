language: node_js
notifications:
  email: false
addons:
  chrome: stable
sudo: false
before_script:
  - npm install -g grunt-cli
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
